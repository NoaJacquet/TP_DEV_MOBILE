import 'package:flutter/material.dart';

class Ecran1 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.blue, // Changez la couleur d'arrière-plan selon vos besoins
    );
  }
}
